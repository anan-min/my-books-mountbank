{
  "inject": "function(request, state, logger) { var sessionId = request.query.sessionId || 'cs_test_unknown'; return { statusCode: 402, headers: { 'Content-Type': 'application/json' }, body: { sessionId: sessionId, status: 'failure', message: 'Payment failed', url: 'https://checkout.stripe.com/pay/' + sessionId } }; }"
}